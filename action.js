var query = require('./query');

var start = function (endpoint, experiment, variant) {
  // Convert endpoint to user id
  // Check if experiment already exists
  // endpoint, experiment, variant
};

module.exports = exports = {
  start: start,
  complete: complete
};
